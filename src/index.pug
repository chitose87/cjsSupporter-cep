doctype html
html
    head
        meta(charset='utf-8')
        //link(rel='stylesheet', href='css/topcoat-desktop-dark.min.css')
        link#hostStyle(rel='stylesheet', href='css/styles.css')
        title
    body.hostElt
        #app
            p
                span build Time
                span !{new Date().toTimeString()}
            button.hostFontSize(@click="reload") reload
            button.hostFontSize(@click="flaDataClear") flaDataClear
            panel-component(ref="panel")

        #content
            p
                span build Time
                span !{new Date().toTimeString()}
            div
                button#btn_test.hostFontSize Call ExtendScript

        script(src='js/libs/CSInterface.js')
        //script(src='js/libs/jquery-2.0.2.min.js')
        script(src='js/themeManager.js')
        //script(src="https://cdn.jsdelivr.net/npm/vue")
        script(src='build.js')
